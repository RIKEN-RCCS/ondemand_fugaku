<%-
  require '/var/www/ood/apps/sys/ondemand_apps/misc/form.rb'
  # spack versions lammps -s
  versions = ["20220623.2"]
  attr = ""
-%>
---
form:
  <%= form_cluster(attr, "fugaku") %>
  <%= form_queue(attr, "fugaku_small") %>
  <%= form_volume(attr) %>
  <%= form_group(attr) %>
  <%= form_fugaku_hours(attr) %>
  <%= form_fugaku_nodes(attr) %>
  <%= form_fugaku_procs(attr) %>
  <%= form_fugaku_threads(attr) %>
  <%= form_mode(attr) %>
  <%= form_version(attr, versions) %>
  <%= form_filename(attr) %>
  <%= form_email(attr, false) %>
attributes:
<%= attr %>

