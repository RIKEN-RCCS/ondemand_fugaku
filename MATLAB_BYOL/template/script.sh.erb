<%= output_xfce(context.global_prepost_vgl) %>

VERSION=R2023a
if [ "${_VIRTUALGL}" != "" ]; then
  if [ "<%= context.global_working_dir %>" != "" ]; then
    TMPDIR=/tmp /vol0004/apps/MATLAB/${VERSION}/bin/matlab -desktop -nosoftwareopengl -sd "<%= context.global_working_dir %>" &
  else
    TMPDIR=/tmp /vol0004/apps/MATLAB/${VERSION}/bin/matlab -desktop -nosoftwareopengl &
  fi
else
  if [ "<%= context.global_working_dir %>" != "" ]; then
    TMPDIR=/tmp /vol0004/apps/MATLAB/${VERSION}/bin/matlab -desktop -sd "<%= context.global_working_dir %>" &
  else
    TMPDIR=/tmp /vol0004/apps/MATLAB/${VERSION}/bin/matlab -desktop &
  fi
fi

${_VIRTUALGL} xfce4-session
fg
