<%
  require '/var/www/ood/apps/sys/ondemand_fugaku/misc/utils.rb'

  added_fugaku_queues = submit_added_fugaku_queues()
  hours = submit_hours(queue, fugaku_small_hours, fugaku_spot_small_hours, NOT_USED, NOT_USED, fugaku_cd_portal_hours, fugaku_pt_hours, NOT_USED, NOT_USED, NOT_USED)
  nodes = submit_nodes(queue, fugaku_small_nodes, fugaku_large_nodes, fugaku_cd_portal_nodes, fugaku_pt_nodes)
  procs = submit_procs(queue, fugaku_small_procs, fugaku_large_procs, fugaku_cd_portal_procs, fugaku_pt_procs)
%>
---
script:
<%= submit_job_name("ood_grads") %>
<%= submit_queue_name(queue) %>
<%= submit_email(email) %>
<%= submit_gpus_per_node(queue, gpus_per_node) %>
<%= submit_native(queue, hours, nodes, procs, NOT_USED, NOT_USED, fugaku_group, fugaku_mode, NOT_USED, NOT_USED, NOT_USED, added_fugaku_queues, NOT_USED) %>
batch_connect:
<%= submit_vnc(staged_root) %>
