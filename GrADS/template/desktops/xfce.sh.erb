<%
  require '/var/www/ood/apps/sys/ondemand_fugaku/misc/utils.rb'
%>

<%= output_xfce(context.gpus_per_node) %>

if [ "${_VIRTUALGL}" = "" ]; then
  xfce4-terminal -e "bash -c \"/opt/grads/bin/grads -b; exec bash\"" -T "GrADS Terminal" &
else
  xfce4-terminal -e "${_VIRTUALGL} bash -c \"/opt/grads/bin/grads -b; exec bash\"" -T "GrADS Terminal" &
fi

${_VIRTUALGL} xfce4-session
fg