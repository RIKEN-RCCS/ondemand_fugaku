<%-
  require '/var/www/ood/apps/sys/ondemand_fugaku/misc/utils.rb'
-%>
---
title: VMD
form:
  <%= form_check("license", "Do you accept the VMD license?", "If you want to use VMD, you need to agree to [the VMD License](https://www.ks.uiuc.edu/Research/vmd/current/LICENSE.html).") %>
  <%= form_cluster("prepost") %>
  <%= form_queue("prepost") %>
  <%= form_prepost1_hours() %>
  <%= form_prepost2_hours() %>
  <%= form_reserved_hours() %>
  <%= form_gpu1_cores() %>
  <%= form_gpu2_cores() %>
  <%= form_mem1_cores() %>
  <%= form_mem2_cores() %>
  <%= form_reserved_cores() %>
  <%= form_gpu1_memory() %>
  <%= form_gpu2_memory() %>
  <%= form_mem1_memory() %>
  <%= form_mem2_memory() %>
  <%= form_reserved_memory() %>
  <%= form_gpus_per_node(true) %>
  <%= form_select("ftype", "File type", ["(auto)", "psf", "pdb", "dcd"], "(auto)", "If nothing is output, select [File] -> [New Molecule] in the VMD window and load the file manually.") %>
  <%= form_input_file(false) %>
  <%= form_email() %>
  <%= form_desktop() %>
attributes:
<%= form_attr() %>
