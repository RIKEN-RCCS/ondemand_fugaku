<%-
  require '/var/www/ood/apps/sys/ondemand_fugaku/misc/utils.rb'
-%>
---
title: VMD
form:
<%= form_check("license", "Do you accept the VMD license?", "If you want to use VMD, you need to agree to [the VMD License](https://www.ks.uiuc.edu/Research/vmd/current/LICENSE.html), and questions regarding the license should be addressed to the VMD Team (described in the license).") %>
<%= form_queue("prepost") %>
<%= form_gpus_per_node(true) %>
<%= form_select("ftype", "File type", ["(auto)", "psf", "pdb", "dcd"], "(auto)", "If nothing is output, select [File] -> [New Molecule] in the VMD window and load the file manually.") %>
<%= form_input_file(false) %>
<%= form_email() %>
<%= form_desktop() %>
attributes:
<%= form_attr() %>
