<%-
  require '/var/www/ood/apps/sys/ondemand_fugaku/misc/utils.rb'
-%>

<%= output_xfce(context.opengl_with_nvidia, context.gpus_per_node, context.queue) %>

xfce4-terminal -e 'bash -c "eval ${_OPENGL_WITH_NVIDIA_OPTION} /vol0004/apps/oss/vmd/bin/vmd <%= context.input_file %>; exec bash"' -T "VMD Terminal" --disable-server &

xfce4-session
