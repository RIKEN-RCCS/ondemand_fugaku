<%-
  require '/var/www/ood/apps/sys/ondemand_fugaku/misc/utils.rb'
-%>
---
title: Job Submitter
form:
<%= form_queue("all", false) %>
<%= form_gpus_per_node(false) %>
<%= form_working_dir() %>
<%= form_commands("e.g. mpiexec ./a.out") %>
<%= form_fugaku_llio("working_dir", "If the executable file does not exist in the working directory, manually transfer the executable file as well. For example, add 'lilo_transfer (executable file)' in commands area.") %>
<%= form_email(false) %>
attributes:
<%= form_attr() %>
