<%-
  require '/var/www/ood/apps/sys/ondemand_apps/misc/submit.rb'
-%>
---
script:
  job_name: ood_nvvp
  queue_name: <%= queue %>
  <%= submit_gpus_per_node(queue, gpus_per_node) %>
  <%= submit_email(email) %>
  <%= submit_native_prepost_gpu(queue, prepost1_hours, gpu1_cores,
  gpu1_memory, prepost2_hours, gpu2_cores, gpu2_memory) %>
batch_connect:
<%= submit_vnc(staged_root) %>
